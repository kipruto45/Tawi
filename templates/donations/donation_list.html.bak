<!DOCTYPE html>
<html lang="en">
<head>
  {% load static %}
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Donations | Tawi Tree Planting Project</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/feather-icons"></script>
  <style>
    body {
      background-image: url('{% static "img/tawi_forest_bg.jpg" %}');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
    }
    .glass {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(34, 197, 94, 0.2);
      border-radius: 18px;
      box-shadow: 0 6px 20px rgba(34, 197, 94, 0.12);
    }
    .btn {
      @apply inline-flex items-center gap-2 px-4 py-2 rounded-lg font-medium transition;
    }
    .btn-green {
      @apply bg-green-600 text-white hover:bg-green-700 shadow-md;
    }
    .btn-outline {
      @apply border border-green-600 text-green-700 hover:bg-green-100;
    }
  </style>
</head>

<body class="min-h-screen flex flex-col items-center justify-between bg-green-50/70">

  <!-- Header -->
  <header class="w-full bg-white/90 backdrop-blur-md border-b border-green-200 shadow-sm p-4 text-center">
    <h1 class="text-2xl font-bold text-green-700 tracking-wide">Donations</h1>
  </header>

  <!-- Main Content -->
  <main class="w-full max-w-6xl px-6 py-12 flex-1 space-y-10">

    {% if request.user.is_staff or request.user.is_superuser %}
    <!-- ADMIN VIEW -->
    <section class="glass p-6">
      <div class="flex justify-between items-center mb-4">
        <h2 class="text-green-700 font-semibold text-lg flex items-center gap-2">
          <i data-feather="list" class="w-5 h-5 text-green-600"></i> All Donations
        </h2>

        <!-- Buttons -->
        <div class="flex gap-3">
          <a href="{% url 'donations:make_donation' %}" class="btn btn-green">
            <i data-feather="plus-circle" class="w-4 h-4"></i> Make Donation
          </a>
          <a href="{% url 'dashboard' %}" class="btn btn-outline">
            <i data-feather="arrow-left" class="w-4 h-4"></i> Back to Dashboard
          </a>
        </div>
      </div>

      <div class="overflow-x-auto">
        <table class="min-w-full text-sm text-left">
          <thead class="bg-green-100 text-green-700 uppercase text-xs">
            <tr>
              <th class="px-4 py-2">#</th>
              <th class="px-4 py-2">Donor</th>
              <th class="px-4 py-2">Email</th>
              <th class="px-4 py-2">Amount (KSh)</th>
              <th class="px-4 py-2">Status</th>
              <th class="px-4 py-2">Date</th>
            </tr>
          </thead>
          <tbody>
            {% for donation in recent_donations %}
            <tr class="border-t border-green-100 hover:bg-green-50 transition">
              <td class="px-4 py-2">{{ forloop.counter }}</td>
              <td class="px-4 py-2">{{ donation.donor.get_full_name|default:donation.donor.username }}</td>
              <td class="px-4 py-2 text-green-600">{{ donation.donor.email }}</td>
              <td class="px-4 py-2 text-green-700 font-medium">{{ donation.amount|floatformat:2 }}</td>
              <td class="px-4 py-2">
                <span class="px-2 py-1 rounded-full text-xs font-semibold
                  {% if donation.status == 'Completed' %}bg-green-600 text-white
                  {% elif donation.status == 'Pending' %}bg-green-200 text-green-800
                  {% else %}bg-gray-100 text-green-600{% endif %}">
                  {{ donation.status }}
                </span>
              </td>
              <td class="px-4 py-2 text-green-600 text-xs">{{ donation.date|date:"M d, Y" }}</td>
            </tr>
            {% empty %}
            <tr><td colspan="6" class="text-center py-4 text-green-600">No donations found 🌱</td></tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </section>

    {% else %}
    <!-- USER VIEW -->
    <section class="glass p-6 text-center space-y-4">
      <h2 class="text-green-700 font-semibold text-lg">🌿 Support Tree Planting</h2>
      <p class="text-green-600">Every donation helps us plant and nurture more trees. Together, we grow a greener future!</p>
      <div class="flex justify-center gap-4 mt-6">
  <a href="{% url 'donations:make_donation' %}" class="btn btn-green">
          <i data-feather="heart" class="w-4 h-4"></i> Make Donation
        </a>
        <a href="{% url 'dashboard' %}" class="btn btn-outline">
          <i data-feather="arrow-left" class="w-4 h-4"></i> Back to Dashboard
        </a>
      </div>
    </section>
    {% endif %}
  </main>

  <!-- Footer -->
  <footer class="w-full text-center text-green-500 text-sm py-4 bg-white/70 backdrop-blur-md border-t border-green-100">
    © 2025 Tawi Tree Planting Project | All Rights Reserved 🌳
  </footer>

  <script>feather.replace();</script>
</body>
</html>
