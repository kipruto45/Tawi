<!DOCTYPE html>
<html lang="en">
<head>
  {% load static %}
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>User Details | Tawi Tree Planting Project</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/feather-icons"></script>
  <style>
    body {
      background-image: url('{% static "img/tawi_forest_bg.jpg" %}');
      background-size: cover;
      background-position: center;
    }
    .card {
      background: rgba(255, 255, 255, 0.97);
      border-radius: 18px;
      box-shadow: 0 4px 24px rgba(34, 197, 94, 0.15);
      border: 1.5px solid #e5e7eb;
    }
    .anim {
      animation: fadeInUp 0.8s ease-out;
    }
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: none; }
    }
  </style>
</head>

<body class="min-h-screen bg-cover bg-center relative flex flex-col items-center justify-start py-10 px-4">
  <div class="absolute inset-0 bg-green-50/80"></div>

  <!-- Header -->
  <div class="relative z-10 text-center mb-6">
  <img src="{% static 'img/tawi_logo.png' %}" alt="Tawi Logo" class="mx-auto w-12 h-12 mb-3 rounded-full shadow border-2 border-green-300" onerror="this.onerror=null;this.src='{% static 'img/tawi_leaf_logo.svg' %}';" />
    <h1 class="text-2xl font-extrabold text-green-700">User Details</h1>
    <p class="text-green-500 text-xs mt-1">View and manage user information efficiently 🌿</p>
  </div>

  <!-- Main Card -->
  <div class="relative z-10 card anim w-full max-w-4xl p-5 md:p-6 text-green-900">

    <!-- User Info -->
    <div class="mb-6 grid grid-cols-1 sm:grid-cols-3 gap-4 items-center">
      <div class="flex flex-col items-center">
        <img src="{{ user.profile.avatar.url|default_if_none:static('img/default_avatar.png') }}" 
             alt="{{ user.get_full_name }}" 
             class="w-24 h-24 rounded-full border-2 border-green-300 shadow mb-3">
        <span class="text-green-700 font-semibold text-sm">{{ user.get_full_name|default:user.username }}</span>
        <span class="text-green-500 text-xs">{{ user.email }}</span>
      </div>

      <div class="sm:col-span-2 grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <h2 class="text-green-700 font-semibold text-xs mb-1">Role</h2>
          <span class="text-xs font-semibold px-2 py-1 rounded-full 
                       {% if user.profile.role == 'Admin' %}bg-green-600 text-white
                       {% elif user.profile.role == 'Field Officer' %}bg-green-200 text-green-800
                       {% elif user.profile.role == 'Partner Institution' %}bg-green-100 text-green-700
                       {% elif user.profile.role == 'Volunteer' %}bg-emerald-50 text-emerald-800
                       {% else %}bg-gray-100 text-green-600{% endif %}">
            {{ user.profile.role }}
          </span>
        </div>

        <div>
          <h2 class="text-green-700 font-semibold text-xs mb-1">Permissions</h2>
          {% if user.profile.permissions.exists %}
            <ul class="text-green-600 text-xs list-disc ml-4">
              {% for perm in user.profile.permissions.all %}
                <li>{{ perm.name }}</li>
              {% endfor %}
            </ul>
          {% else %}
            <span class="text-green-500 text-xs">No special permissions</span>
          {% endif %}
        </div>

        <div>
          <h2 class="text-green-700 font-semibold text-xs mb-1">Last Updated</h2>
          <span class="text-green-500 text-xs">{{ user.profile.updated_at|date:"M d, Y H:i" }}</span>
        </div>

        <div>
          <h2 class="text-green-700 font-semibold text-xs mb-1">Date Joined</h2>
          <span class="text-green-500 text-xs">{{ user.date_joined|date:"M d, Y" }}</span>
        </div>
      </div>
    </div>

    <!-- Action Buttons -->
    <div class="flex justify-center gap-3 mb-4">
      <a href="{% url 'role_edit' user.id %}"
         class="bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 transition-all py-2.5 px-4 rounded-md font-semibold shadow flex items-center gap-2 text-white text-sm">
        <i data-feather="edit-2" class="w-4 h-4"></i> Edit User
      </a>
      <a href="{% url 'role_delete' user.id %}" 
         onclick="return confirm('Are you sure you want to delete this user?');"
         class="bg-red-500 hover:bg-red-600 transition-all py-2.5 px-4 rounded-md font-semibold shadow flex items-center gap-2 text-white text-sm">
        <i data-feather="trash-2" class="w-4 h-4"></i> Delete User
      </a>
    </div>

    <!-- Navigation -->
    <div class="flex justify-center gap-3">
  <a href="{% url 'accounts:role_management' %}"
         class="bg-green-100 hover:bg-green-200 text-green-700 text-sm px-4 py-2 rounded-md shadow flex items-center gap-1 transition">
        <i data-feather="arrow-left" class="w-4 h-4"></i> Back to Roles
      </a>
      <a href="{% url 'dashboard:dashboard' %}"
         class="bg-green-600 hover:bg-green-700 text-white text-sm px-4 py-2 rounded-md shadow flex items-center gap-1 transition">
        <i data-feather="home" class="w-4 h-4"></i> Dashboard
      </a>
    </div>

    <!-- Footer -->
    <p class="text-center text-[10px] text-green-400 mt-6">
      © 2025 Tawi Tree Planting Project | Empowering Green Leaders 🌳
    </p>

  </div>

  <script>feather.replace();</script>
</body>
</html>
