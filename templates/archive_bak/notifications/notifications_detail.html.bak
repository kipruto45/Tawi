<!DOCTYPE html>
<html lang="en">
<head>
  {% load static %}
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Notification Detail | Tawi Tree Planting Project</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/feather-icons"></script>
  <style>
    body {
      background-image: url('{% static "img/tawi_forest_bg.jpg" %}');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
    }
    .glass {
      background: rgba(255,255,255,0.92);
      backdrop-filter: blur(10px);
      border:1px solid rgba(34,197,94,0.2);
      border-radius:18px;
      box-shadow:0 6px 20px rgba(34,197,94,0.12);
    }
  </style>
</head>
<body class="min-h-screen flex flex-col items-center justify-start p-8">

  <!-- Header -->
  <header class="mb-6 text-center">
  <img src="{% static 'img/tawi_logo.png' %}" alt="Tawi Logo" class="w-12 h-12 mx-auto rounded-full border border-green-300 mb-2" onerror="this.onerror=null;this.src='{% static 'img/tawi_leaf_logo.svg' %}';" />
    <h1 class="text-2xl font-extrabold text-green-700">Notification Details</h1>
    <p class="text-green-600 text-sm mt-1">View full details of this notification 🌿</p>
  </header>

  <!-- Notification Card -->
  <section class="glass w-full max-w-3xl p-6 space-y-4">
    <div class="flex justify-between items-center">
      <h2 class="text-lg font-semibold text-green-700">{{ notification.title }}</h2>
      <span class="text-xs px-2 py-1 rounded-full
                   {% if notification.status == 'Unread' %}bg-yellow-100 text-yellow-700
                   {% elif notification.status == 'Read' %}bg-green-100 text-green-700
                   {% else %}bg-gray-100 text-gray-700{% endif %}">
        {{ notification.status }}
      </span>
    </div>

    <div>
      <p class="text-green-700 font-medium">Sent By</p>
      <p class="text-green-600">{{ notification.sender.get_full_name|default:notification.sender.username }}</p>
    </div>

    <div>
      <p class="text-green-700 font-medium">Recipient</p>
      <p class="text-green-600">{{ notification.recipient.get_full_name|default:notification.recipient.username }}</p>
    </div>

    <div>
      <p class="text-green-700 font-medium">Message</p>
      <p class="text-green-600 whitespace-pre-wrap">{{ notification.message|default:"No message content." }}</p>
    </div>

    <div>
      <p class="text-green-700 font-medium">Sent At</p>
      <p class="text-green-600 text-xs">{{ notification.created_at|date:"M d, Y H:i" }}</p>
    </div>

    <div class="flex justify-between mt-4">
      <a href="{% url 'notifications_list' %}"
         class="bg-green-100 hover:bg-green-200 text-green-700 text-sm px-4 py-2 rounded-md shadow flex items-center gap-2 transition">
        <i data-feather="arrow-left" class="w-4 h-4"></i> Back to Notifications
      </a>
      {% if notification.status == 'Unread' %}
      <a href="{% url 'notifications_mark_read' notification.id %}"
         class="bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 text-white text-sm px-4 py-2 rounded-md shadow flex items-center gap-2 transition">
        <i data-feather="check" class="w-4 h-4"></i> Mark as Read
      </a>
      {% endif %}
    </div>
  </section>

  <!-- Footer -->
  <footer class="mt-6 text-center text-green-400 text-xs">
    © 2025 Tawi Tree Planting Project | Notifications
  </footer>

  <script>feather.replace();</script>
</body>
</html>
